# GTK Theme

Injects GTK-like CSS (Adwaita) to style web pages to match your Linux desktop.

- Only active on Linux (automatically disabled on Windows/macOS).
- Styles buttons, text inputs, and scrollbars (if the engine supports it) to match the Adwaita theme.
